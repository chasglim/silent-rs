[package]
name = "silent-utils"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared utility code for SILENT core crates."

[dependencies]
rand = { version = "0.8", features = ["small_rng"] }
rand_chacha = "0.3"
rkyv = { version = "0.7", features = ["validation", "strict"] }
thiserror = "1.0"

[features]
blake2b-neon = []
